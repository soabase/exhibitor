package com.netflix.exhibitor.core.activity;

/**
 * The support queue groups. The names are meaningless
 * except as a hint as to what the queues are used for
 */
public enum QueueGroups
{
    MAIN,
    IO
}
